Synth: 
- Im Sequenzer wenn man die fx pro Kanal setzen will fehlt die apply oder on off Funktion
- Warum wird Web Midi verwendet wenn es eine Desktop Anwendung ist ? Bau es um dass die Desktop Version auch nur dementsprechende Methoden und Funktionen benutzt weil alles andere wäre ineffezient
- Der Demo Lizens Key geht nicht
- Wenn man auf Connect mit Omnitribe drückt passiert nichts
- Die Ki Funktionen die abhängig von dem api key sind funktionieren nicht bzw der api key wird nicht akzeptiert
- 
- in der pattern library unter Tools soll auch eine kleine Vorschau oder anhör Funktion vorhanden sein kamit man weiß welches pattern man lädt
-Bearbeitungsfunktionen implementieren wie Rename vom Pattern

-Die Stabilität und Belastbarkeit der Audio engine soll verbessert werden Weil ich öfter Laggs hatte
-Im Sequenzer bei Auswahl der Steps auch die gesamten Steps anzeigen auf einer seite, es soll auch eine Möglichkeit geben die Global Stepanzahl für ein Projekt zu setzen und nicht das man bei jedem pattern das auch noch anpassen muss, bei den pattern ist schon gut aber eine höhere ebene die dann für alle pattern diese regel anwendet

-Mixer Tab:
 Die Funktionen wie Master FX und Audio sidechain sollen aufklappbar sein wenn man sie braucht oder wenn man den Master FX button oder Audio Sidechain button drückt sollen die Funktionen in einem seperaten fenster sichtbar sein und nicht den kompletten mixer verdrängen, weil aktuell kann man den mixer nicht sehen

-ℹ
16 Slice(s) erstellt — Direct-Assign in Pad-Slots noch nicht implementiert. Slice-Buffer via 'sample-slicer:apply'-Event verfuegbar.

-wenn ich über korg bank eine esx Datei lade wo sampler und pattern sind dann sehe ich auch die originalen steps im pattern und alles ist zugewiesen aber wenn ich Play drücke dann wechselt er den pattern ohne ihn abzuspielen und wenn ich über Import gehe dann sieht er die steps nicht nur die sampler
